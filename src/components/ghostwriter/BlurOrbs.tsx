/**
 * Ambient shapes for the Ghostwriter background.
 * Kept asymmetrical and subdued so the page feels designed,
 * not like a generic AI-product orb stack.
 */
export function BlurOrbs() {
  return null;
}
