# Limited Free-only portfolio release

This deployment supersedes the paid-SaaS launch scope. Paid inference remains disabled; the broader `verify:release` and historical financial evidence are retained, not declared passed. The continuation request authorizes one local Free canary only after real authentication, target schema and durable control verification. That call has not been performed; production inference is not authorized.

Continuation state (2026-09-07): the owner attests that the dedicated local Groq key belongs to the Free plan. Supabase and Vercel CLI account checks report logged out, and no exact Supabase project or Vercel linkage is configured. Browser account access was denied. Local configuration selects `portfolio-free` with AI and unconfigured login methods disabled. No hosted migrations, OAuth changes, deployment, keys or billing changes were made. `vercel.json` pins the tested Next.js webpack build; production environment configuration and plan/project verification still require account access. Local secrets and test artifacts are explicitly excluded by `.vercelignore`.

The composer now uses privately fetched verified session/allowance state for its sign-in, remaining-trial, exhausted and paused presentation. Production admission remains authoritative. Browser UI acceptance uses explicitly synthetic auth/rewrite responses and is separate from the real isolated GoTrue authentication proof; neither establishes real GitHub OAuth or Groq execution. Scroll integrity preserves the native document scroller and requires bottom-edge screenshots as well as the scroll tests.

## Implemented boundary

`GHOSTWRITER_RELEASE_PROFILE=portfolio-free` uses the existing authenticated gateway, server-owned Groq model/request, atomic SQL reservation/dispatch claim, account-scoped idempotency and durable pause. No demo inference route, fallback, automatic inference retry, additional provider, or zero-price fiction exists. The visitor's actual text is sent. The separate Tolkien portfolio film is unchanged.

Migration `202609070004_ghostwriter_portfolio_free.sql` enforces 25 non-recycled trial slots; 10 lifetime starts/account; 3/account/rolling day; 1/account/rolling minute; 2/global/rolling minute; 25/global/rolling day; 250/global/lifetime; one globally active operation. Admission holds capacity; a committed dispatch permanently counts as a start. A provably unclaimed reservation may be released. Timeout/ambiguous completion retains its start and reservation and pauses new work. Replays do not dispatch again, including while paused.

The first eligible server-verified sign-in claims an available trial transactionally. New Free recipients do not get paid approval. A persisted normalized-email digest prevents deletion/recreation of the same email from granting another trial; it is an identifier, not proof of a unique human. Financial/recipient rows have no cascading Auth deletion. Different verified identities can consume availability, but cannot exceed global quotas.

Body cap: 10,000 bytes while reading; existing 2,000-character and conservative UTF-8 input restrictions remain. Provider output parameter: `max_completion_tokens=2000`; deadline: 30 seconds; one request, redirect rejection, no automatic inference retry. Existing price arithmetic is retained as an estimate/history field (750 micro-USD maximum under the old reviewed estimate), not a charge or a prerequisite paid-token certification. Free plan enforcement is an external dependency, not something this estimate proves.

Email login is hidden and rejected by default. GitHub OAuth uses Supabase PKCE, a 10-minute HttpOnly verifier cookie, fixed callback/return destinations and `user:email` scope; no repository scopes or stored GitHub provider tokens. Issuer, audience, signature, expiry, verified identity and durable session checks are unchanged. Logout no longer shares rewrite quotas. Invalid sessions clear stale auth cookies; uncertain revocation remains an error. Remaining allowance is fetched privately; identity changes clear output across tabs. Rewrite errors retain input.

## Actual verification and remaining evidence

Use `.tmp/release/portfolio-evidence.json` generated by `verify:portfolio` for the exact source/build and results. Local proofs include the production SQL boundary exercised through two gateway processes, actual provider request serialization with outbound fetch replaced, account/global window boundaries, replay, disconnect/pause and datastore failure. Authentication proof uses real isolated GoTrue/PostgREST/PostgreSQL and actual Next HTTP handlers. It does not establish GitHub account configuration or SMTP delivery.

A native browser permission failure is BLOCKED, not a quota defect or PASS. Run the prepared GitHub Actions portfolio job in an authorized runner, or record owner browser acceptance against the exact target: sign in with a legitimate non-team GitHub account, refresh, logout, verify captured-token rejection, retain input on unavailable errors, and inspect remaining allowance. No real account plan, deployed key owner, target schema or live rewrite is currently verified. Deployment/live enablement therefore remains BLOCKED pending the actions below.

## Owner setup, in order

1. **Verify real plans, without changing shared organizations.** In Groq Console, select the intended organization, inspect Billing for **Free** (not Developer/paid credits), then Projects and API Keys to match the deployed key's project and organization. Record only organization/project/key-reference metadata and plan evidence. Restrict that project to Only Allow `openai/gpt-oss-20b`. No inference-key API used here proves the organization plan. In Supabase, verify the project's **organization** subscription is Free and that project limits allow it. In Vercel, verify the actual personal project is on **Hobby**, not a Pro trial/team. No hosting project linkage was found in this repository; Vercel is a proposed target, not a discovered deployment.
2. **Configure visitor auth.** In the same Supabase project, enable GitHub. The GitHub OAuth app's callback is `https://SUPABASE_PROJECT_REF.supabase.co/auth/v1/callback`. In Supabase URL Configuration, allow only `https://PORTFOLIO_HOST/api/ghostwriter/auth/callback`; set Site URL to the exact HTTPS app origin. Enable legitimate new OAuth users, not anonymous users. Keep email/password, phone/SMS and unused providers disabled unless deliberately needed. Set bounded Supabase Auth rate limits. Leave `GHOSTWRITER_EMAIL_LOGIN_ENABLED=false`; only enable it after bounded custom SMTP delivers to a legitimate non-team address. Do not add visitors to the Supabase team.
3. **Review credentials and previews.** Put GitHub client secret in Supabase provider settings only. Runtime names: `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY` (public identity), `SUPABASE_SERVICE_ROLE_KEY` (privileged server), `GROQ_API_KEY` (verified Free-project inference), `GHOSTWRITER_SECURITY_SECRET`, `GHOSTWRITER_FINGERPRINT_SECRET`. Use different signing/fingerprint keys and separate preview/test credentials/projects. Never copy production provider/admin/signing keys into previews or `NEXT_PUBLIC_*`. Root signing compromise compromises derived purposes. Rotate real exposed/shared keys only with separate approval; no real rotation occurred here.
4. **Apply forward migrations with AI disabled outside the DB.** Confirm the exact target and migration history first. Do not reset a database or restore stale quota history. See commands below. The new migration starts with the existing paused/default state; it never enables itself.
5. **Deploy with `AI_ENABLED=false`.** Configure `NEXT_PUBLIC_SITE_URL`, `GHOSTWRITER_RELEASE_PROFILE`, `GROQ_FREE_ORGANIZATION_ID`, `GROQ_FREE_PROJECT_ID`, `GHOSTWRITER_GITHUB_LOGIN_ENABLED`, `GHOSTWRITER_EMAIL_LOGIN_ENABLED`, `GHOSTWRITER_ABUSE_STORE_MODE`, `GHOSTWRITER_TRUST_PROXY`, `GHOSTWRITER_CLIENT_IP_HEADER`, the existing model/resource settings, and the secret names above. Verify the real ingress overwrites the selected client-IP header; a flag alone is not evidence. Disable paid email/SMS, storage extensions, log drains, monitoring add-ons and unrelated billable integrations if any are found; none was changed here.
6. **Record matching Free-plan review while paused.** Operator commands require protected `PGSERVICE`/`.pgpass`, exact database name and durable `deployment_id`; dry-run is default. `configure-portfolio` records an owner-attested evidence reference valid for at most 30 days. This is not automatic billing verification. The schema record must match runtime organization/project metadata. `enable-portfolio` is a deliberate audited action, has at most a 24-hour lease and refuses unresolved operations. No automatic resume or lifetime reset exists.
7. **Accept auth and verify the disabled deployment.** Complete browser acceptance and target evidence. `preflight:portfolio` checks source/build identity, target metadata/schema/grants and required owner attestations read-only. It exits nonzero on missing evidence. Configure `GHOSTWRITER_PORTFOLIO_TARGET_EVIDENCE` to a private JSON file containing sourceCommit/sourceTreeSha256/buildSha256 from local evidence, verifiedAt, targetOrigin, deploymentId, supabaseProjectRef, groqOrganizationId/groqProjectId; plan labels groqPlan=`Free`, supabasePlan=`Free`, hostingPlan=`Hobby`; and booleans credentialProjectVerified, previewIsolationVerified, authAcceptanceVerified, ingressVerified, noPaidAddonsVerified. Do not include secrets or secret hashes.
8. **Authorize one real Free operation separately.** Sign in first to claim the owner's finite trial. Prepare a fixed UUID canary with the operator command; this temporarily admits only its matching account/key. With plan/schema/auth evidence and separate live authorization, deliberately enable the Free profile (DB and server flag), run the canary once with the same durable run ID, verify replay and stored operation, and confirm pause. Never rerun with a new ID after ambiguity. After success, `complete-portfolio-canary` retains its history, disables only its temporary admission restriction and stays paused; explicitly enable the portfolio afterward. All visitors still share the original 25 slots/250 starts. No live call has occurred under this request.

## Commands

Run from `/Users/malmeida/Documents/mockup/ghostwritter`. Uppercase placeholders below are metadata to replace, never credentials.

```bash
npm ci
npm run dev
# http://127.0.0.1:4003/second-voice
npm run verify:portfolio
npm run test:portfolio
npm run test:scroll
npm run preflight:portfolio
```

Owner-approved Supabase target setup, using the supported installed Supabase CLI and secure interactive login (initialize CLI config only if absent):

```bash
supabase init
supabase login
supabase link --project-ref SUPABASE_PROJECT_REF
supabase migration list
supabase db push --dry-run
# Only after inspecting the exact target/history and authorizing migration:
supabase db push
```

If existing migrations were applied outside the CLI, reconcile migration history with the owner before `db push`; do not blindly reapply/reset. Read the durable database ID using the protected operator service: `psql -X -Atc 'select deployment_id from public.ghostwriter_ai_control where singleton'`.

Owner-approved Vercel Hobby deployment (CLI login/link are owner actions; secrets belong in the exact project's production settings, not command-line arguments):

```bash
vercel login
vercel link
vercel project inspect
# Confirm Hobby and AI_ENABLED=false, then authorize:
vercel --prod
```

Read-only/dry-run operator examples; add `--service=SERVICE --database-id=DATABASE_UUID --execute` only with explicit owner approval and matching PGSERVICE:

```bash
node scripts/release/operator.mjs configure-portfolio --target=postgres --organization=GROQ_ORG --project=GROQ_PROJECT --evidence=OWNER_REVIEW_REFERENCE --valid-until=REVIEW_EXPIRY_ISO --approve=verified-free-only --reason=free_plan_review
node scripts/release/operator.mjs prepare-canary --target=postgres --account=OWNER_UUID --run=FIXED_RUN_UUID --reason=free_smoke
node scripts/release/operator.mjs enable-portfolio --target=postgres --approve=verified-free-only --reason=authorized_free_release
node scripts/release/canary.mjs --portfolio --target=https://PORTFOLIO_HOST/ --account=OWNER_UUID --run=FIXED_RUN_UUID
node scripts/release/operator.mjs pause --target=postgres --reason=owner_pause
node scripts/release/operator.mjs complete-portfolio-canary --target=postgres --run=FIXED_RUN_UUID --reason=verified_free_smoke
```

The canary's live branch additionally requires `--approve=one-operation --execute --database=postgres --service=SERVICE --database-id=DATABASE_UUID` and an ephemeral `GHOSTWRITER_CANARY_ACCESS_TOKEN` supplied securely to the process, never pasted in chat or command arguments. It verifies the durable run/account binding, reuses its key, and pauses in `finally`. Target acceptance must also inspect the settled row and provider usage; a response alone is not external-plan evidence.

Normal owner review/commit, if Git writes are restricted here:

```bash
git diff --check
git diff
git status --short
git add .
node scripts/release/scan-secrets.mjs
git commit -m "Add limited Free-only portfolio release"
npm run verify:portfolio
```

No push is required or performed by these local commands. Re-generate evidence after committing; source changes invalidate prior evidence.

## Availability and external facts

Reviewed 2026-09-07: [Groq billing](https://console.groq.com/docs/billing-faqs), [rate limits](https://console.groq.com/docs/rate-limits), [project controls](https://console.groq.com/docs/projects), [model permissions](https://console.groq.com/docs/model-permissions); [Supabase organization billing](https://supabase.com/docs/guides/platform/billing-on-supabase), [default SMTP restrictions](https://supabase.com/docs/guides/auth/auth-smtp), [GitHub/PKCE setup](https://supabase.com/docs/guides/auth/social-login/auth-github); [Vercel Hobby](https://vercel.com/docs/plans/hobby).

These documents describe product behavior, not this owner's actual accounts. Free limits can pause service; Supabase Free inactivity and project limits can affect availability. Vercel Hobby is for eligible personal/noncommercial use; usage exhaustion can require waiting, not buying more capacity. Do not upgrade or rotate accounts/keys to evade limits. No promise of uninterrupted service or total invoice guarantee is made. A plan change, wrong/shared credential or paid infrastructure add-on invalidates Free-only assumptions. Restore/rollback requires AI paused outside restored state until the durable quotas are reconciled.
